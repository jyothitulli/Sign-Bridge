# Use the installer (handles TF + TFJS conflicts on Python 3.13):
#   npm run setup:training
#   python training/setup_env.py
#
# Or minimal (prepare scripts only):
#   pip install -r training/requirements-base.txt

-r requirements-core.txt
